# Documents

Place public academic documents in this directory using these exact filenames:

- `Xin_Meng_CV.pdf` — CV
- `growthmamba-paper.pdf` — GrowthMamba paper or manuscript
- `growthmamba-slides.pdf` — conference slides

The homepage checks for the two GrowthMamba files and keeps their resource labels in a “Coming soon” state until the files are present. The CV controls already point to `files/Xin_Meng_CV.pdf`; add the CV before publishing the link to conference materials.

For future work, use stable, lowercase filenames and update the corresponding Selected Research entry in `index.html`.
